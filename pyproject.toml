[project]
name = "stripe-sandbox-be"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Trong Hai", email = "truongtronghai@gmail.com" }]
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.43.91",
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.1",
    "pre-commit>=4.6.2",
    "uvicorn>=0.52.4",
]

[tool.fastapi]
entrypoint = "src.main:app"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["ruff>=0.16.6"]
